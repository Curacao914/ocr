"""
导出和输出功能详细说明
"""

# 💾 导出和输出功能详细说明

## 概述

GLM-OCR工具支持多种输出格式，可满足不同使用场景：

| 格式 | 文件扩展名 | 推荐用途 | 是否包含图片 |
|------|---------|--------|---------|
| Markdown | .md | 编辑、版本控制、网站发布 | ✓ 支持 |
| Word文档 | .docx | 分享、打印、进一步编辑 | ✓ 自动嵌入 |

## Markdown格式导出

### 特点

- ✅ 纯文本格式，易于版本管理
- ✅ 支持Git/GitHub集成
- ✅ 可用任何文本编辑器编辑
- ✅ 易于转换为其他格式
- ✅ 网络可读性好

### 文件结构

```markdown
---
生成时间: 2026-03-26 10:30:45
生成工具: GLM-OCR OCR转换工具 v1.0.0
---

# 文档标题

## 章节1

这是第一个段落的内容...

### 小节

- 列表项1
- 列表项2
- 列表项3

## 表格示例

| 列A | 列B |
|-----|-----|
| 值1 | 值2 |
| 值3 | 值4 |

## 代码块

```python
def hello():
    print("Hello World")
```
```

### 包含的元素

1. **标题体系**
   - H1 (#) - 文档主标题
   - H2 (##) - 一级章节
   - H3 (###) - 二级小节
   - H4+ (####) - 更深层级

2. **文本格式**
   - 普通段落
   - **粗体**（**text**）
   - *斜体*（*text*）
   - `代码`（`code`）

3. **列表**
   - 无序列表（-/*/+）
   - 有序列表（1. 2. 3.）
   - 嵌套列表（缩进）

4. **表格**
   - 自动从识别结果转换
   - 完整保留结构
   - 支持合并单元格信息

5. **代码块**
   ```
   ```language
   code here
   ```
   ```

6. **元数据**
   - 生成时间
   - 工具版本信息
   - YAML格式头

### 使用场景

- 📖 **技术文档**：编写规范和指南
- 🌐 **网站内容**：用Jekyll/Hugo等静态网站生成器
- 📝 **笔记管理**：Notion、Obsidian等笔记应用
- 🔄 **版本控制**：提交到Git进行版本管理
- 📖 **电子书**：用Pandoc转换为PDF/EPUB
- 💬 **社交发布**：Reddit、GitHub、Medium等

### 示例处理流程

```
原始PDF
   ↓
GLM-OCR识别
   ↓
转换为Markdown
   ↓
用编辑器修改调整
   ↓
发布到网站/GitHub
```

## Word文档导出

### 特点

- ✅ 易于分享和打印
- ✅ 支持进一步编辑和排版
- ✅ 自动应用样式
- ✅ 可嵌入图片和媒体
- ✅ 兼容所有操作系统

### 文件结构

```
完整的Word文档
├── 元数据页
│   ├── 生成时间
│   └── 工具版本信息
│
├── 标题页（可选）
│
├── 内容
│   ├── 标题（自动样式）
│   ├── 段落
│   ├── 列表
│   ├── 表格
│   └── 图片
│
└── 附加信息
    └── 文档属性
```

### 自动应用的格式

1. **标题样式**
   - Heading 1 - 主标题（14pt，加粗，蓝色）
   - Heading 2 - 子标题（12pt，加粗）
   - Heading 3 - 小节（11pt，加粗）

2. **列表样式**
   - 项目符号列表 - 一级
   - 项目符号列表（二级） - 嵌套
   - 编号列表 - 按序号

3. **表格格式**
   - 浅色表头背景
   - 边框线
   - 自动列宽调整

4. **段落格式**
   - 行间距：1.15倍
   - 段前距：0pt
   - 段后距：12pt

### 使用场景

- 📄 **正式文档**：报告、合同、提案
- 🖨️ **打印输出**：易于打印和分享
- 💼 **办公应用**：Word/Office深度编辑
- 📧 **邮件发送**：附件形式分享
- 📊 **数据表**：导入到其他应用
- 💾 **存档文档**：长期保存和检索

### 注意事项

- 第一次打开Word可能需要几秒钟
- 修改后记得保存
- 导出的图片可能需要重新调整大小
- 某些复杂表格可能需要手动调整

## 输出选项详解

### 1️⃣ 仅Markdown

**何时选择**：
- 需要文本版本
- 用于网站或博客
- 需要版本控制

**输出内容**：
- 单个 `.md` 文件
- 包含所有文字内容
- 支持表格和代码块

**文件大小**：通常最小

### 2️⃣ 仅Word文档

**何时选择**：
- 需要分享文档
- 需要进一步编辑
- 需要打印输出

**输出内容**：
- 单个 `.docx` 文件
- 自动应用格式
- 支持图片嵌入

**文件大小**：中等

### 3️⃣ 两种格式都导出

**何时选择**：
- 不确定用途
- 需要灵活使用
- 想同时保留两个版本

**输出内容**：
- 同时生成 `.md` 和 `.docx`
- 内容基本相同
- 各有优势

**文件大小**：最大

## 包含图片选项

### 什么时候要勾选"包含图片"

✅ **应该包含图片**：
- 原文档包含印章或签名
- 需要完整保留文档外观
- 包含重要的表格截图
- 包含二维码或条形码
- 需要论证文档真实性

❌ **不需要包含图片**：
- 仅需提取文字信息
- 想减小文件大小
- 原文档是纯文字扫描
- 图片质量很差或无用

### 图片处理方式

| 导出格式 | 图片保存方式 | 备注 |
|---------|------------|------|
| Markdown | 保留URL链接 | 需要网络访问 |
| Word | 嵌入到文档 | 文件更大，离线可用 |

## 导出后的文件位置

### 默认位置
```
项目根目录/output/
└── 日期_时间_文件名.md
└── 日期_时间_文件名.docx
```

### 自定义位置
编辑 `.env` 文件：
```
OUTPUT_DIR=/path/to/your/folder
```

### 查看文件
- **macOS**：Finder → 拖到项目文件夹 → output
- **Windows**：File Explorer → 项目文件夹 → output
- **Linux**：`ls output/` 或 `explorer output/(Ubuntu)`

## 导出后的进一步处理

### Markdown文件

```bash
# 查看文件
cat output/file.md

# 用编辑器打开
# macOS
open -a "Visual Studio Code" output/file.md

# Windows
code output/file.md

# Linux
vim output/file.md
```

### Word文档

```bash
# 直接打开
# macOS
open output/file.docx

# Windows
start output/file.docx

# Linux
libreoffice output/file.docx
```

### 格式转换

```bash
# Markdown转PDF
pandoc input.md -o output.pdf

# Markdown转HTML
pandoc input.md -o output.html

# Markdown转Word
pandoc input.md -o output.docx

# 需要先安装Pandoc
```

## 输出质量优化

### 提升导出质量

1. **输入文档清晰度**
   - 扫描质量 ≥ 300dpi
   - 避免倾斜或旋转
   - 保持原纸张大小

2. **选择合适的导出格式**
   - 文字为主 → Markdown
   - 需要排版 → Word文档
   - 需要打印 → Word文档

3. **导出后的编辑**
   - Word：使用样式调整
   - Markdown：用编辑器修改

### 常见问题

**Q: 导出的文件打不开？**
A: 
- 检查文件是否完整
- 更新Microsoft Office或LibreOffice
- 用在线Word编辑器打开

**Q: 表格格式混乱？**
A:
- 在Word中右键选择"调整表格"
- 手动调整列宽
- 可能需要重新排版

**Q: 图片显示不出来？**
A:
- 确认已勾选"包含图片"
- 检查网络连接（对于URL链接）
- 刷新文档（Word中按Ctrl+A再删除键）

**Q: 文件很大？**
A:
- 取消"包含图片"选项
- 删除不需要的内容
- 用7-Zip等工具压缩

## 导出最佳实践

### ✅ 推荐做法

1. **文件命名**
   ```
   好：invoice_2026-03-26.docx
   不好：文件1.docx
   ```

2. **批量导出**
   - 使用文件夹模式处理所有文件
   - 一次导出所有结果
   - 统一放在output文件夹

3. **版本管理**
   ```
   output/
   ├── v1/
   │   └── document_v1.docx
   ├── v2/
   │   └── document_v2.docx
   └── final/
       └── document_final.docx
   ```

4. **备份**
   - 定期备份output文件夹
   - 重要文件保存多份
   - 用云存储备份

### ❌ 避免做法

- ❌ 将输出文件保存到temp目录（会被清理）
- ❌ 不备份重要导出文件
- ❌ 使用复杂特殊字符作为文件名
- ❌ 直接修改临时文件而不备份

## 导出工作流完整示例

### 场景：处理100份影像化合同

**步骤1**：准备
```
1. 将100份合同PDF放在 ~/contracts/ 文件夹
2. 打开应用，进入"⚙️ 配置"，初始化客户端
```

**步骤2**：处理
```
1. 进入"📁 处理文件"
2. 选择"文件夹"模式
3. 输入 ~/contracts/
4. 点击"开始处理"
5. 等待完成（约10-30分钟）
```

**步骤3**：导出
```
1. 进入"💾 导出结果"
2. 选择"Word文档"格式
3. 勾选"包含图片"
4. 点击"导出结果"
5. 所有文件输出到./output/
```

**步骤4**：后续处理
```
1. 打开./output/文件夹
2. 逐个用Word打开检查
3. 手动校对和修改
4. 保存最终版本
5. 上传到文档管理系统
```

---

**需要帮助？** 查看应用内的"❓ 帮助"标签或查阅README.md

**最后更新**：2026-03-26
