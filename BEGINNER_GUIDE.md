"""
完全新手指南 - 从0开始使用GLM-OCR工具
"""

# 🎯 完全新手指南

## 前置需求

### 1. 安装Python

#### macOS用户
```bash
# 方式1：用Homebrew安装（推荐）
brew install python3

# 方式2：从官网下载
# https://www.python.org/downloads/

# 验证安装
python3 --version
```

#### Windows用户
```bash
# 从官网下载
https://www.python.org/downloads/

# 安装时注意勾选"Add Python to PATH"
# 验证安装
python --version
```

#### Linux用户
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install python3 python3-pip

# CentOS/RHEL
sudo yum install python3 python3-pip

# 验证安装
python3 --version
```

### 2. 获取API密钥

1. 访问 https://open.bigmodel.cn
2. 点击右上角"注册/登录"
3. 用邮箱或手机注册账号
4. 完成身份验证
5. 进入"API管理"或"开发者中心"
6. 创建API密钥
7. 复制密钥备用

**⚠️ 注意**：妥善保管API密钥，不要分享给他人

## 第一步：项目配置（10分钟）

### 步骤1：打开项目文件夹

```bash
# macOS/Linux用户
cd 案卷ocr

# Windows用户
# 用文件浏览器打开"案卷ocr"文件夹
```

### 步骤2：配置API密钥

#### macOS/Linux用户
```bash
# 复制配置文件
cp .env.example .env

# 用文本编辑器打开
nano .env
# 或
vim .env
```

找到这一行：
```
ZHIPU_API_KEY=your_api_key_here
```

改为：
```
ZHIPU_API_KEY=你从智谱AI复制的密钥
```

按 `Ctrl+O` 保存，`Ctrl+X` 退出（nano编辑器）

#### Windows用户
```bash
# 复制配置文件
copy .env.example .env
```

用记事本打开 `.env` 文件：
1. 右键单击 `.env` 文件
2. 选择"打开方式" → "记事本"
3. 找到 `ZHIPU_API_KEY=your_api_key_here`
4. 改为 `ZHIPU_API_KEY=你的密钥`
5. 按 `Ctrl+S` 保存
6. 关闭记事本

### 步骤3：安装依赖

#### macOS用户
```bash
# 在项目目录中打开终端
# 可以用 Command+Space 搜索"终端"或在项目文件夹中右键

# 运行
pip3 install -r requirements.txt

# 等待安装完成（可能需要几分钟）
```

#### Windows用户
```bash
# 在项目文件夹中按 Shift+右键
# 选择"在此处打开PowerShell"或"在此处打开命令提示符"

# 运行
pip install -r requirements.txt

# 等待安装完成
```

#### Linux用户
```bash
# 打开终端
cd /path/to/案卷ocr

# 运行
pip3 install -r requirements.txt
```

## 第二步：启动应用（简单3步）

### macOS/Linux用户

**方式一：使用启动脚本（推荐）**
```bash
chmod +x start.sh
./start.sh
```

**方式二：直接运行**
```bash
python3 main.py
```

### Windows用户

**方式一：使用启动脚本（推荐）**
- 双击 `start.bat` 文件

**方式二：命令行运行**
```bash
python main.py
```

### 应用启动完成

当看到以下输出时，表示应用已启动：
```
=========================================
GLM-OCR 文档转换工具 v1.0.0
=========================================
启动Gradio界面...
```

**自动打开浏览器**访问：http://localhost:7860

如果浏览器没有自动打开，手动打开浏览器，在地址栏输入上述地址。

## 第三步：首次使用（15分钟教程）

### 1️⃣ 初始化客户端（第一次必做）

在打开的应用界面中：

1. 点击顶部的 **"⚙️ 配置"** 标签
2. 在"ZHIPU API密钥"输入框中粘贴你的API密钥
3. 点击"初始化客户端"按钮

等待返回"客户端初始化成功 ✓"信息

### 2️⃣ 选择处理文件方式

进入 **"📁 处理文件"** 标签

选择以下任一方式：

#### 方式A：处理单个文件（最简单）
1. 在"处理模式"中选择"单个文件"
2. 点击"上传文件"按钮
3. 选择一个PDF或JPG/PNG图片文件
4. 点击"开始处理"按钮
5. 等待（通常5-30秒）
6. 处理完成后会显示结果

#### 方式B：批量处理文件夹
1. 在"处理模式"中选择"文件夹"
2. 在"文件夹路径"输入框中输入文件夹路径
   - macOS/Linux示例：`/Users/username/Documents/pdfs`
   - Windows示例：`C:\Users\username\Documents\pdfs`
3. 点击"开始处理"按钮
4. 等待（根据文件数量）
5. 全部处理完成后显示结果

### 3️⃣ 预览结果

进入 **"👁️ 预览结果"** 标签

1. 从下拉菜单选择要预览的文件
2. 点击"预览"按钮
3. 右侧会显示OCR识别的内容

### 4️⃣ 导出结果

进入 **"💾 导出结果"** 标签

1. 选择导出格式：
   - **Markdown** - 纯文本格式，易于编辑
   - **Word文档** - .docx格式，便于分享和打印
   - **两种格式** - 同时导出两种格式

2. 如果识别的是扫描件，勾选"包含图片"以保留印章等
3. 点击"导出结果"按钮
4. 等待导出完成

导出的文件会保存在项目的 **`output/`** 目录中

## 常见问题速查表

### ❌ 问题：API密钥错误
```
❌ 初始化失败: API密钥错误
```
**解决**：
- 检查密钥是否正确复制（无多余空格）
- 确认密钥未过期
- 重新从智谱AI官网复制一份

### ❌ 问题：文件过大
```
❌ 文件过大: file.pdf (120MB, 限制: 50MB)
```
**解决**：
- 系统会自动拆分，无需手动处理
- 如果仍有问题，用PDF工具手动拆分

### ❌ 问题：识别失败
```
✗ 失败: 无法识别文件
```
**解决**：
- 确认文件格式为PDF/JPG/PNG
- 检查文件是否损坏
- 尝试提高图片质量（重新扫描）

### ❌ 问题：权限错误
```
❌ 导出失败: 写入权限不足
```
**解决**：
- 检查 `output/` 文件夹是否存在
- 检查磁盘是否有足够空间
- 关闭占用输出文件的应用

### ✅ 问题：如何找到输出文件？
输出文件保存在项目的 `output/` 文件夹中

- **macOS**：Finder → 项目文件夹 → output
- **Windows**：文件浏览器 → 项目文件夹 → output
- **命令行**：`cd output && ls` (macOS/Linux) 或 `cd output && dir` (Windows)

## 实战示例

### 例子1：识别一张发票照片

1. 用手机拍照一张发票
2. 将照片保存到电脑
3. 打开应用
4. 进入"📁 处理文件"标签
5. 选择"单个文件"模式
6. 上传照片
7. 点击"开始处理"
8. 等待3-5秒
9. 进入"💾 导出结果"标签
10. 导出为Word文档（含图片）
11. 从 `output/` 文件夹打开.docx文件

### 例子2：识别一个PDF合同

流程完全相同，选择PDF文件即可

### 例子3：批量处理100份发票

1. 将所有发票照片放在一个文件夹（如：`~/invoice_photos/`）
2. 打开应用
3. 进入"📁 处理文件"标签
4. 选择"文件夹"模式
5. 输入文件夹路径
6. 点击"开始处理"
7. 等待系统处理所有文件（可能10-30分钟）
8. 全部完成后进入"💾 导出结果"
9. 导出所有文件

## 🎓 进阶技巧

### 如何修改输出位置

编辑 `.env` 文件：
```
OUTPUT_DIR=/path/to/my/folder
```

### 如何查看处理日志

日志文件保存在 `temp/logs/` 文件夹

打开最新的 `ocr_*.log` 文件查看详细信息

### 如何加快处理速度

1. 使用文件夹模式而不是单个处理
2. 确保网络连接稳定
3. 使用高质量的扫描件（≥300dpi）

## 💡 小贴士

- 💰 **成本控制**：1元可处理约2000张A4图片或200份10页PDF
- ⚡ **提升速度**：上游扫描质量更好，识别速度更快
- 🖼️ **保留印章**：导出时勾选"包含图片"
- 📁 **组织文件**：处理前将文件按类型分类放入不同文件夹
- 🔄 **自动重试**：识别失败时系统会提示，可重新处理

## 🚨 重要警告

⚠️ **不要做**：
- ❌ 分享或上传你的API密钥
- ❌ 在代码中硬编码API密钥
- ❌ 将.env文件提交到Git
- ❌ 删除temp目录中的日志（除非磁盘满了）

## 🆘 获取帮助

如果遇到问题：

1. **查看内置帮助**
   - 点击应用中的"❓ 帮助"标签

2. **查看文档**
   - README.md - 完整文档
   - QUICKSTART.md - 快速开始
   - CHANGELOG.md - 版本信息

3. **查看日志**
   - 文件位置：`temp/logs/ocr_*.log`
   - 可提供给技术支持诊断问题

4. **联系支持**
   - 访问：https://chat.z.ai
   - 或：https://bigmodel.cn

## ✨ 下一步

现在你已经准备好了！

**建议流程**：
1. ✅ 已安装Python
2. ✅ 已配置API密钥
3. ✅ 已安装依赖
4. ✅ 已启动应用
5. 🚀 **现在开始使用吧！**

---

**需要帮助？** 查看README.md或点击应用中的"❓ 帮助"标签

**祝使用愉快！🎉**
